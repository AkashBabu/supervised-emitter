{
    "name": "supervised-emitter",
    "version": "0.5.0",
    "description": "A event emitter library that supports middleware functionality which can be used to supervise the events flow in the system / application.",
    "main": "built/src/index",
    "typings": "types/src/index",
    "scripts": {
        "_test": "cross-env TS_NODE_FILES=true mocha --require ts-node/register test/**/*",
        "_test:exit": "npm run _test -- --exit",
        "test": "cross-env NODE_ENV=test npm run _test:exit",
        "test:dev": "cross-env NODE_ENV=test npm run _test -- -w",
        "test:grep": "cross-env NODE_ENV=test npm run _test -- -g ",
        "coverage": "nyc npm run test",
        "build": "tsc",
        "lint:fix": "tslint --fix --config tslint.json src/index.ts",
        "lint": "tslint --config tslint.json src/index.ts",
        "benchmark:clean": "node benchmark/clean.js",
        "benchmark:report": "radargun benchmark/**/*.bench.js",
        "benchmark:template": "node benchmark/template.js",
        "benchmark": "npm run build && npm run benchmark:clean && npm run benchmark:report && npm run benchmark:template",
        "status": "git status",
        "coveralls": "npm run coverage && nyc report --reporter=text-lcov | coveralls",
        "load-test": "cross-env TS_NODE_FILES=true mocha --require ts-node/register load/**/*"
    },
    "engines": {
        "node": ">=8"
    },
    "repository": {
        "type": "git",
        "url": "git+https://github.com/AkashBabu/supervised-emitter.git"
    },
    "keywords": [
        "event",
        "emitter",
        "scalable",
        "middleware",
        "glob"
    ],
    "author": "001akashbabu@gmail.com",
    "license": "MIT",
    "bugs": {
        "url": "https://github.com/AkashBabu/supervised-emitter/issues"
    },
    "homepage": "https://github.com/AkashBabu/supervised-emitter#readme",
    "precommit": [
        "lint:fix",
        "lint",
        "coverage",
        "load-test",
        "build",
        "benchmark",
        "status"
    ],
    "nyc": {
        "extension": [
            ".ts"
        ],
        "exclude": [
            "**/*.d.ts"
        ],
        "include": [
            "src/**/*"
        ]
    },
    "devDependencies": {
        "@types/chai": "^4.2.3",
        "@types/mocha": "^5.2.7",
        "@types/node": "^12.7.12",
        "chai": "^4.1.2",
        "cli-progress": "^3.3.1",
        "coveralls": "^3.0.6",
        "cross-env": "^6.0.0",
        "delay": "^4.3.0",
        "mocha": "^5.2.0",
        "nodemon": "^1.19.3",
        "nyc": "^12.0.2",
        "pre-commit": "^1.2.2",
        "radargun": "^1.0.1",
        "rimraf": "^2.6.2",
        "source-map-support": "^0.5.13",
        "ts-node": "^8.0.2",
        "tslint": "^5.20.0",
        "typescript": "^3.6.4"
    },
    "dependencies": {
        "node-lfu-cache": "^5.0.0"
    }
}
